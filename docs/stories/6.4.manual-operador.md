# Story 6.4 — MANUAL.md (operador)

| Campo | Valor |
|---|---|
| **ID** | 6.4 |
| **Épico** | 6 |
| **Status** | Draft |
| **Assignee** | @pm (Morgan) com revisão do @architect |
| **Estimativa** | 1 h |
| **Prioridade** | P0 (bloqueia release — FR-18) |
| **Rastreia** | PRD FR-18 |
| **Depende de** | 5.2 |
| **Bloqueia** | 6.6 |

## User Story
> **Como** operador de importação, **quero** um manual em português cobrindo todo o uso da ferramenta, **para** configurar, rodar e interpretar sem depender de ajuda externa.

## Acceptance Criteria

| # | Critério |
|---|---|
| AC1 | `MANUAL.md` na raiz do repositório |
| AC2 | Seção 1 — **Pré-requisitos**: SO suportado, acesso de rede ao PostgreSQL de IPTU, arquivo `application.properties` provisionado |
| AC3 | Seção 2 — **Configuração**: anatomia do `application.properties` (chave-a-chave em PT, referenciando `application.properties.exemplo`) |
| AC4 | Seção 3 — **Fluxo completo**: passo-a-passo `validar-conexao` → `mapear` → editar `mapping.json` → `validar` → `importar`, com exemplos de comandos reais |
| AC5 | Seção 4 — **Interpretação do resumo** (bloco ASCII ilustrado) e do `.log` (exemplos de linhas) |
| AC6 | Seção 5 — **Exit codes** em tabela (`0`, `1`, `2`) |
| AC7 | Seção 6 — **Troubleshooting**: os 5 erros mais comuns esperados (conexão recusada, senha errada, arquivo não encontrado, coerção falhou, PENDENTE bloqueando) com soluções |
| AC8 | Linkado no `README.md` (seção "Desenvolvimento" ou nova seção "Uso") |
| AC9 | Revisado por um operador real (ou pelo @architect simulando) antes de release |

## Tasks

- [ ] Rascunhar `MANUAL.md`
- [ ] Incluir screenshots ASCII reais (gerados pelo sistema)
- [ ] Revisão com @architect
- [ ] Adicionar referência no `README.md`

## Dev Notes

- Basear exemplos em fixtures reais (mesmas da Story 6.3).
- Evitar jargão: manual é para operador de TI, não para dev.
- Incluir seção "glossário" opcional se houver termos do domínio não óbvios.

## Testing
- Validação subjetiva: um terceiro consegue seguir o manual do zero? Idealmente testar com 1 pessoa antes de release.

## File List
_Preenchido pelo autor._

## DoD
AC1–AC9 verdes; `@po *close-story 6.4`.
