# Story 1.2 — Scaffold Quarkus + estrutura de pacotes + convenções

| Campo | Valor |
|---|---|
| **ID** | 1.2 |
| **Épico** | 1 — Fundação da CLI e conexão |
| **Status** | Ready for Review |
| **Assignee** | @dev (Dex) |
| **Estimativa** | 0,5 h (scaffold gerado pela IA) |
| **Prioridade** | P0 |
| **Rastreia** | PRD FR-01; Arquitetura §2, §8 |
| **Depende de** | 1.1 |
| **Bloqueia** | 1.3, 1.4, 1.5 e todas as stories de código |

## User Story
> **Como** dev, **quero** o projeto Quarkus scaffolded com estrutura de pacotes e convenções definidas, **para que** todas as stories subsequentes tenham base para codificar.

## Acceptance Criteria

| # | Critério |
|---|---|
| AC1 | `pom.xml` gerado via `quarkus-maven-plugin:create` com Quarkus 3.x e Java 21 |
| AC2 | `groupId=br.com.arxcode.tematica.geo`, `artifactId=importacao-geo` |
| AC3 | Extensões instaladas: `quarkus-picocli`, `quarkus-jdbc-postgresql`, `quarkus-agroal`, `quarkus-jackson` |
| AC4 | Dependência adicional: `org.dhatim:fastexcel-reader` (última versão estável) |
| AC5 | Estrutura de pacotes criada conforme Arquitetura §8: `cli/`, `excel/`, `catalogo/`, `mapeamento/`, `geracao/`, `dominio/`, `config/` |
| AC6 | `CONVENCOES.md` na raiz descreve: naming (UpperCamelCase classes, lowerCamelCase métodos, SCREAMING_SNAKE_CASE constantes), imports absolutos (sem `import *`), `@RegisterForReflection` obrigatório em POJOs de domínio |
| AC7 | `./mvnw quarkus:dev` inicia (porta 8080 ou mensagem de startup detectada no log), encerra com `Ctrl+C` e retorna `exit code 0` sem stack trace |
| AC8 | `./mvnw test` roda (mesmo com 0 testes — infra pronta para NFR-09) |
| AC9 | Workflow CI atualizado: substitui o placeholder da 1.1 por `setup-java@v4` + `./mvnw -B verify` |
| AC10 | Testcontainers configurado no pom como dependência de teste (prepara 1.5, 2.3) |

## Tasks

- [x] `mvn io.quarkus.platform:quarkus-maven-plugin:3.34.5:create` — resolveu para Quarkus `3.34.6` (latest disponível); `groupId=br.com.arxcode.tematica.geo`, `artifactId=importacao-geo`
- [x] Adicionar `fastexcel-reader 0.18.4` ao `<dependencies>`
- [x] Adicionar Testcontainers `1.20.4` (`postgresql`, `junit-jupiter`) ao `<dependencies>` com scope `test`
- [x] Criar diretórios vazios dos 7 pacotes (com `.gitkeep`)
- [x] Escrever `CONVENCOES.md`
- [x] Atualizar `.github/workflows/ci.yml` — `setup-java@v4` temurin-21 + `./mvnw -B verify`
- [x] Rodar `./mvnw quarkus:dev` local — inicia em 0.741s, exibe CLI help, encerra exit 0 (AC7 ✅)
- [x] Rodar `./mvnw test`; BUILD SUCCESS (0 testes — infra pronta)

## Pré-condições (confirmadas)

| # | Decisão | Valor |
|---|---|---|
| 1 | Versão Quarkus | `3.34.5` |
| 2 | groupId | `br.com.arxcode.tematica.geo` |
| 3 | artifactId | `importacao-geo` |
| 4 | Java | 21 LTS |

✅ **Gate liberado** — todas as decisões confirmadas. @dev pode iniciar.

## Fora do Escopo (OUT)

- ❌ Conexão real com banco de dados PostgreSQL → Story 1.5
- ❌ Implementação de qualquer subcomando (`validar-conexao`, `mapear`, etc.) → Stories 1.3, 1.4, 1.5
- ❌ Build nativo GraalVM (`native-image`) → Story 6.1
- ❌ Leitura de planilhas Excel → Story 2.1
- ❌ Testes unitários com lógica de negócio → Stories 2.x em diante

## Riscos

| Risco | Probabilidade | Mitigação |
|---|---|---|
| `fastexcel-reader` requer `reflect-config.json` para native-image | Média | Arquitetura §8 já prevê placeholder em `META-INF/native-image/`; documentar na Story 6.1 |
| Testcontainers exige Docker no CI (AC10) | Baixa | Testcontainers só executado nos testes de integração (Stories 1.5+); AC10 apenas adiciona a dependência ao `pom.xml` sem ativar containers agora |
| Incompatibilidade Quarkus 3.34.5 + Java 21 no build CI | Baixa | Usar `setup-java@v4` com `distribution: temurin` e `java-version: 21`; fixar versão |

## Dev Notes

- Java 21 LTS (default do Quarkus 3 atual).
- `fastexcel-reader` não é extensão Quarkus — dependência Maven comum.
- Remover o código Hello World gerado (`GreetingResource`, `GreetingResourceTest`, etc.) — vira ruído.
- Manter wrappers `mvnw`, `mvnw.cmd`, `.mvn/wrapper/` **versionados** (permitem build sem Maven instalado; decisão deferida da Story 1.1).
- Package base: `br/com/arxcode/tematica/geo/` (espelha o groupId).

## Testing / Validação

- [x] `./mvnw clean package` verde — BUILD SUCCESS
- [x] Estrutura de pacotes presente: 7 pacotes em `src/main/java/br/com/arxcode/tematica/geo/`
- [x] `./mvnw test` verde — BUILD SUCCESS (0 testes, infra pronta)
- [ ] CI verde com o novo workflow — aguardar run no GitHub Actions após push
- [x] `CONVENCOES.md` na raiz com naming, imports absolutos e `@RegisterForReflection`

## File List

**Criados:**
- `pom.xml`
- `mvnw`, `mvnw.cmd`, `.mvn/wrapper/maven-wrapper.properties`
- `.tool-versions`
- `CONVENCOES.md`
- `src/main/resources/application.properties`
- `src/main/java/br/com/arxcode/tematica/geo/cli/MainCommand.java`
- `src/main/java/br/com/arxcode/tematica/geo/cli/.gitkeep`
- `src/main/java/br/com/arxcode/tematica/geo/excel/.gitkeep`
- `src/main/java/br/com/arxcode/tematica/geo/catalogo/.gitkeep`
- `src/main/java/br/com/arxcode/tematica/geo/mapeamento/.gitkeep`
- `src/main/java/br/com/arxcode/tematica/geo/geracao/.gitkeep`
- `src/main/java/br/com/arxcode/tematica/geo/dominio/.gitkeep`
- `src/main/java/br/com/arxcode/tematica/geo/config/.gitkeep`
- `src/main/docker/Dockerfile.jvm`, `Dockerfile.legacy-jar`, `Dockerfile.native`, `Dockerfile.native-micro`

**Modificados:**
- `.github/workflows/ci.yml` (placeholder → setup-java@v4 + ./mvnw -B verify)

## Quality Gate

### CodeRabbit Integration

Esta é a **primeira story com código Java** — CodeRabbit entra em vigor a partir daqui (conforme Story 1.1 §8.1).

- @dev executa CodeRabbit antes de commitar: `wsl bash -c '~/.local/bin/coderabbit --prompt-only -t uncommitted'`
- Issues CRITICAL: bloqueiam o commit
- Issues HIGH: documentar e corrigir antes do push

### Agentes e ações

| Agente | Ação |
|---|---|
| @sm (River) | Drafted this story |
| @po (Pax) | `*validate-story-draft 1.2` |
| @dev (Dex) | Implementar AC1–AC10 |
| @po (Pax) | `*close-story 1.2` após ACs verdes |

## DoD

- [x] AC1–AC10 verdes (AC9 CI — aguardar Actions run após push)
- [x] Testing / Validação completa
- [x] File List preenchida pelo @dev
- [ ] CodeRabbit: zero issues CRITICAL
- [ ] CI verde no GitHub Actions — aguardar push
- [ ] `@po *close-story 1.2` executado

## Change Log

| Data | Agente | Evento | Detalhes |
|---|---|---|---|
| 2026-04-20 | @sm (River) | Drafted | Story criada a partir do PRD FR-01, Arquitetura §2 e §8 |
| 2026-04-23 | @po (Pax) | Validated — GO (7.5/10) | Status `Draft → Ready`. Aplicadas 6 melhorias: (1) versão Quarkus `3.34.5`; (2) groupId `br.com.arxcode.tematica.geo`; (3) seção Pré-condições formal; (4) seção OUT adicionada; (5) Riscos documentados; (6) AC7 reescrito com critério objetivo; (7) DoD expandido; (8) seção Quality Gate / CodeRabbit adicionada. |
| 2026-04-23 | @dev (Dex) | Implementado | Quarkus resolveu 3.34.6 (latest). Scaffold movido para raiz. fastexcel-reader 0.18.4, Testcontainers 1.20.4. 7 pacotes com .gitkeep. MainCommand placeholder (picocli exige @TopCommand). application.properties configurado para container Docker. `quarkus:dev` inicia em 0.741s. `./mvnw test` BUILD SUCCESS. CI atualizado. Status → `Ready for Review`. |
