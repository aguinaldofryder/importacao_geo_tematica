# Story 2.2 — Domínio: enums e records (Campo, Alternativa, GrupoCampo, Fluxo, Tipo)

| Campo | Valor |
|---|---|
| **ID** | 2.2 |
| **Épico** | 2 |
| **Status** | Draft |
| **Assignee** | @dev |
| **Estimativa** | 0,3 h |
| **Prioridade** | P0 |
| **Rastreia** | Arquitetura §8 |
| **Depende de** | 1.2 |
| **Bloqueia** | 2.3, 3.1, 3.2 |

## User Story
> **Como** sistema, **quero** POJOs imutáveis representando o catálogo, **para** ter uma camada tipada entre banco e lógica de mapeamento.

## Acceptance Criteria

| # | Critério |
|---|---|
| AC1 | Enum `Fluxo { TERRITORIAL, SEGMENTO }` com método `funcionalidade()` retornando `"TERRENO"` / `"SEGMENTO"` (match com `grupocampo.funcionalidade`) |
| AC2 | Enum `Fluxo` com `tabelaPrincipal()` e `tabelaRespostas()` mapeando para os nomes físicos |
| AC3 | Enum `Tipo { TEXTO, DECIMAL, DATA, MULTIPLA_ESCOLHA }` |
| AC4 | Record `Campo(long id, String descricao, Tipo tipo, boolean ativo, long idGrupoCampo)` |
| AC5 | Record `Alternativa(long id, String descricao, long idCampo)` |
| AC6 | Record `GrupoCampo(long id, String funcionalidade)` |
| AC7 | Todos anotados `@RegisterForReflection` (NFR-02 / preparação para native) |
| AC8 | Testes básicos: equality (via record), Jackson round-trip |

## Tasks

- [ ] Criar 2 enums
- [ ] Criar 3 records em `dominio/`
- [ ] Anotar reflection
- [ ] Testes

## Dev Notes

- Records Java 21 — pure data, zero lógica.
- `@RegisterForReflection(targets={Campo.class, Alternativa.class, GrupoCampo.class})` em uma classe marker.
- `Fluxo.TERRITORIAL.funcionalidade() == "TERRENO"` (há essa assimetria — documentar).

## Testing
- [ ] Equality
- [ ] JSON round-trip

## File List
_Preenchido pelo @dev._

## DoD
AC1–AC8 verdes; `@po *close-story 2.2`.
