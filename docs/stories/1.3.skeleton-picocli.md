# Story 1.3 — Esqueleto picocli (subcomandos stub)

| Campo | Valor |
|---|---|
| **ID** | 1.3 |
| **Épico** | 1 |
| **Status** | Draft |
| **Assignee** | @dev |
| **Estimativa** | 0,3 h |
| **Prioridade** | P0 |
| **Rastreia** | PRD FR-01 |
| **Depende de** | 1.2 |
| **Bloqueia** | 1.4, 1.5, 3.4, 3.5, 4.5 |

## User Story
> **Como** dev, **quero** os 4 subcomandos picocli criados como stubs, **para que** as stories de comandos finais (`validar-conexao`, `mapear`, `validar`, `importar`) tenham ponto de ancoragem.

## Acceptance Criteria

| # | Critério |
|---|---|
| AC1 | `MainCommand` anotado `@QuarkusMain` e `@CommandLine.Command(name="importacao-geo", mixinStandardHelpOptions=true)` |
| AC2 | 4 subcommand stubs: `ValidarConexaoCommand`, `MapearCommand`, `ValidarCommand`, `ImportarCommand`, cada um em arquivo próprio em `cli/` |
| AC3 | Cada stub imprime `"[TODO Story X.Y] comando <nome> ainda não implementado"` e retorna exit `0` |
| AC4 | `java -jar target/quarkus-app/quarkus-run.jar --help` lista os 4 subcomandos com descrição em PT |
| AC5 | Cada comando tem teste `@QuarkusTest` que instancia e executa via `PicocliCommandLineFactory`, esperando exit 0 e mensagem TODO |
| AC6 | Mensagens em português (NFR-01) |

## Tasks

- [ ] Criar `MainCommand.java`
- [ ] Criar `ValidarConexaoCommand.java` (stub)
- [ ] Criar `MapearCommand.java` (stub)
- [ ] Criar `ValidarCommand.java` (stub)
- [ ] Criar `ImportarCommand.java` (stub)
- [ ] Testes `@QuarkusTest` para os 4
- [ ] Confirmar `--help` lista tudo em PT

## Dev Notes

- picocli + Quarkus: usar `@TopCommand` + subcommands.
- Convenção: `@CommandLine.Command(name="mapear", description="Gera mapping.json...")`.
- Evitar lógica de negócio — apenas ponto de extensão.

## Testing
- [ ] Testes unit passam
- [ ] `--help` output revisado manualmente

## File List
_Preenchido pelo @dev._

## DoD
AC1–AC6 verdes; `@po *close-story 1.3`.
