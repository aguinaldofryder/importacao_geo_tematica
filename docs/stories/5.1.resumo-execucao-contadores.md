# Story 5.1 — ResumoExecucao (contadores)

| Campo | Valor |
|---|---|
| **ID** | 5.1 |
| **Épico** | 5 — Resumo pós-importação |
| **Status** | Draft |
| **Assignee** | @dev |
| **Estimativa** | 0,3 h |
| **Prioridade** | P0 |
| **Rastreia** | PRD FR-14, NFR-09 |
| **Depende de** | 4.5 |
| **Bloqueia** | 5.2 |

## User Story
> **Como** sistema, **quero** acumular contadores durante o `importar`, **para** produzir o resumo auditável exigido pelo PRD.

## Acceptance Criteria

| # | Critério |
|---|---|
| AC1 | `ResumoExecucao` com métodos: `incrementarLido()`, `registrarSucesso()`, `registrarErro()`, `registrarUpdatePrincipal()`, `registrarRespostaAtualizada()`, `registrarRespostaInserida()` |
| AC2 | Métodos: `iniciar()` (captura timestamp de início) e `finalizar()` (captura fim) |
| AC3 | Getter `duracao()` retorna `Duration` |
| AC4 | Getter `totalRespostas()` = atualizadas + inseridas |
| AC5 | Snapshot imutável via `toResumoImutavel()` retornando um record com todos os contadores — usado para renderização (Story 5.2) |
| AC6 | Single-thread em v1 (documentado); `AtomicInteger` opcional para futuro |
| AC7 | Testes unit extensivos (módulo crítico NFR-09): cada incremento, duração, snapshot |

## Tasks

- [ ] Implementar `ResumoExecucao` em `geracao/`
- [ ] Record imutável `ResumoSnapshot`
- [ ] Integrar no `ImportarCommand` (Story 4.5) via edit
- [ ] Testes unit

## Dev Notes

- Contadores primitivos `int`; single-thread.
- `java.time.Instant` para início/fim.

## Testing
- **Módulo crítico** — cobertura alta.

## File List
_Preenchido pelo @dev._

## DoD
AC1–AC7 verdes; `@po *close-story 5.1`.
