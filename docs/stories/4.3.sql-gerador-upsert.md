# Story 4.3 — `SqlGeradorUpsert` (tabela de respostas, colunas dinâmicas)

| Campo | Valor |
|---|---|
| **ID** | 4.3 |
| **Épico** | 4 — Geração de SQL e execução |
| **Status** | Draft |
| **Assignee** | @dev |
| **Estimativa** | 2,5 h |
| **Prioridade** | P0 |
| **Rastreia** | PRD FR-12 (UPSERT em tabela de respostas), FR-13 (erro por linha vai ao log; pipeline continua), CON-03 (SQL como arquivo texto, sem `PreparedStatement`), CON-04 (PostgreSQL-only — sintaxe `INSERT ... ON CONFLICT ... DO UPDATE` é PG-específica); NFR-01 (mensagens em PT), NFR-09 (cobertura ≥ 70 % em módulo crítico — meta local ≥ 80 %) |
| **Depende de** | 2.2 (`Fluxo`/`Tipo`), 3.1 (`Mapeamento` / `ColunaDinamica`), 4.1 (`Coercionador` / `ResultadoCoercao`), 4.2 (`SqlEscape`, `LinhaMapeada`, padrão arquitetural `função pura final class`) |
| **Bloqueia** | 4.5 (`ImportarCommand`) |

## User Story

> **Como** sistema gerador de SQL, **quero** produzir, para cada **célula dinâmica não-vazia** de cada linha mapeada da planilha, um par de instruções `DELETE FROM <tabela_respostas> WHERE referencia=<ref> AND idcampo=<id>; INSERT INTO <tabela_respostas> (id, referencia, valor, idcampo, idalternativa) VALUES (nextval('aise.s_<tabela>_id'), <ref>, <valor>, <id>, <idalt>);` — usando `Coercionador` para validar tipo da célula e o helper privado `serializarValor` para mapear o `ResultadoCoercao` na coluna `valor varchar(250)` (TEXTO/DECIMAL/DATA serializados como string; MULTIPLA_ESCOLHA usa `valor=''` literal e popula `idalternativa`) — **para** que o arquivo `.sql` final seja sintaticamente válido, idempotente em re-execução (DELETE tolerante a 0 linhas), seguro contra SQL injection (CON-03) e capaz de reportar erros por linha como dado (FR-13).

## Acceptance Criteria

| # | Critério |
|---|---|
| AC1 | Existe `record ResultadoUpsert(List<String> sqls, List<String> erros)` em `br.com.arxcode.tematica.geo.geracao` com fábricas estáticas `public static ResultadoUpsert sucesso(List<String> sqls)` e `public static ResultadoUpsert falha(List<String> erros)`. Construtor canônico aplica `List.copyOf(...)` em ambas (normaliza `null` → `List.of()`); fábricas validam invariantes: (a) `sucesso` aceita lista vazia (caso degenerado AC8 — linha sem dinâmicas é válida) e exige `sqls != null` e nenhum elemento `null`/blank, sob pena de `IllegalArgumentException` PT; (b) `falha` exige `erros != null && !erros.isEmpty()` e nenhum elemento `null`/blank, sob pena de `IllegalArgumentException` PT. Método de conveniência `public boolean ok()` retornando `erros.isEmpty()`. Análogo a `ResultadoUpdate` (Story 4.2) — diferença chave: `sql:String` → `sqls:List<String>` (uma linha planilha gera N UPSERTs, um por célula dinâmica não-vazia). |
| AC2 | Existe `public final class SqlGeradorUpsert` em `br.com.arxcode.tematica.geo.geracao` (sem CDI, função pura, construtor público sem argumentos) com método `public ResultadoUpsert gerar(LinhaMapeada linha, Mapeamento mapeamento, Fluxo fluxo, Coercionador coercionador)`. A injeção do `Coercionador` por parâmetro segue o padrão da Story 4.2 AC5 / Story 4.1 AC8 (consumidor decide ciclo de vida; mantém testabilidade). Validação dos argumentos: qualquer parâmetro `null` → `IllegalArgumentException` com mensagem PT (programação defeituosa, não dado ruim — não vira `ResultadoUpsert.falha`). |
| AC3 | **Iteração e filtragem.** Para cada entrada `(header → ColunaDinamica)` em `mapeamento.colunasDinamicas()` (ordem de iteração — ver AC6): (a) **Pular** entradas com `coluna.status() != MAPEADO` (PENDENTE/IGNORADO não têm `idcampo` válido — ver `ColunaDinamica.java:33-39` + Story 3.3 §StatusMapeamento). (b) **Pular** entradas cujo valor da célula `linha.celulasDinamicas().get(header)` seja `null`, vazio ou whitespace puro (`String.isBlank()`) — divergência intencional vs. Story 4.2 (lá `null` vira `NULL` literal; aqui pulamos porque emitir UPSERT com todos os valores `NULL` polui a tabela de respostas). (c) Demais entradas vão para coerção (passo seguinte). |
| AC4 | **Coerção tipada.** Para cada célula dinâmica processada (passou nos filtros do AC3), invocar `coercionador.coagir(valorCelula, coluna.tipo(), coluna.alternativas())`. O `Coercionador` (Story 4.1) já trata os 4 tipos (`TEXTO`, `DECIMAL`, `DATA`, `MULTIPLA_ESCOLHA`) e devolve `ResultadoCoercao.literalSql()` com o literal pronto para concatenação (com aspas para TEXTO/DATA, sem aspas para DECIMAL/idAlternativa). |
| AC5 | **Falha de coerção em qualquer célula dinâmica processada** → `ResultadoUpsert.falha(erros)` com mensagens PT no formato `"Coluna '<header>': <erro do Coercionador>"`. **Todas** as falhas da linha são acumuladas (não interrompe na primeira), permitindo ao operador corrigir N células em uma só passada na planilha — espelha política da Story 4.2 AC9. **Nenhum SQL é emitido** quando há ao menos um erro (UPSERT parcial mascara bugs e o PRD prefere falha de linha sobre dado parcial — alinhado a `AGENTS.md` §Domínio: "Código imobiliário ausente → log de erro, pula a linha inteira (incluindo campos dinâmicos)"). Falha na coerção do `codigoImovel` (defesa em profundidade — TEXTO sempre passa para entradas não-blank) também acumula como `"Código do imóvel: <erro>"`. |
| AC6 | **Forma exata do SQL gerado por célula dinâmica não-vazia.** Para cada célula válida, `gerar()` produz **DOIS strings** consecutivos na lista `sqls`, nesta ordem: (1) `DELETE FROM aise.<tabelaRespostas> WHERE referencia = <referencia> AND idcampo = <idCampo>;` — (2) `INSERT INTO aise.<tabelaRespostas> (id, referencia, valor, idcampo, idalternativa) VALUES (nextval('aise.<sequenceRespostas>'), <referencia>, <valorSerializado>, <idCampo>, <idAlternativaOuNULL>);`. Onde: (a) `<referencia>` vem de `linha.colunasFixas().get(fluxo.colunaChave())`, escapada com `SqlEscape.aspas` (CON-03 — vetor `O'Brien`/injection cobertos no AC10/AC11); (b) `<valorSerializado>` produzido pelo helper privado `serializarValor(ResultadoCoercao, Tipo)` — ver AC11; (c) `<idAlternativaOuNULL>` é o id quando Tipo == MULTIPLA_ESCOLHA, senão o literal `NULL`. Linha começa com `DELETE FROM aise.` e segundo SQL com `INSERT INTO aise.` (asserção AC15). |
| AC7 | **Determinismo da ordem dos UPSERTs.** A ordem dos elementos em `ResultadoUpsert.sqls()` segue a ordem de iteração de `mapeamento.colunasDinamicas()`. Caller que construa `Mapeamento` programaticamente **deve** usar `LinkedHashMap` para preservar a garantia (esta classe não força cópia defensiva do `Mapeamento`; pendência herdada de `ISSUE-4.2-01`, fora de escopo). Em produção real, Jackson preserva ordem das chaves do `mapping.json`, que veio do classificador da Story 2.4 ordenado conforme planilha — caminho real OK. |
| AC8 | **Caso degenerado.** Se `mapeamento.colunasDinamicas()` estiver vazio **OU** se nenhuma célula dinâmica passar nos filtros do AC3 (todas pendentes ou vazias), retorna `ResultadoUpsert.sucesso(List.of())` — **lista vazia, não falha**. **Divergência intencional vs. Story 4.2 AC8** (que retornava `falha`): linha sem respostas dinâmicas é situação **válida** no fluxo (o orquestrador da 4.5 ainda gerará o `UPDATE` da tabela principal via 4.2). Documentado no Javadoc com referência à 4.2 para contraste. |
| AC9 | **Acumulação plena de erros (fecha ISSUE-4.2-02).** N falhas de coerção em uma única linha geram lista de N mensagens PT — caminho de acumulação plenamente exercitável agora porque os tipos `DECIMAL`/`DATA`/`MULTIPLA_ESCOLHA` falham para entradas inválidas (`Coercionador.java:108-109,124,130,136,147,152`). Teste dedicado força ao menos 2 falhas simultâneas em uma linha (ex.: DECIMAL inválido + DATA inválida) e valida que a lista contém ambas, com prefixo `"Coluna '<header>': "` em cada uma. |
| AC10 | **Segurança CON-03 — escape de aspas em `referencia`.** Código `"O'Brien"` em `linha.codigoImovel()` produz UPSERTs com `VALUES ('O''Brien', ...)`. Tentativa de injection (`"1'; DROP TABLE x; --"`) vira literal inerte `'1''; DROP TABLE x; --'`. Coerção do `codigoImovel` como `Tipo.TEXTO` (mesmo padrão da Story 4.2 AC7/AC11) — delega ao `Coercionador.coagirTexto` que delega a `SqlEscape.aspas`. Teste dedicado (não parametrizado, smoke explícito). |
| AC11 | **Helper privado `serializarValor(ResultadoCoercao resultado, Tipo tipo)` em `SqlGeradorUpsert`** retornando `record ValorSerializado(String valorParaColunaValor, String idAlternativaOuNull)`. Regras por Tipo: (a) **TEXTO** → `(SqlEscape.aspas(resultado.literalSql()), "NULL")` — `literalSql()` já vem com aspas externas; remover via `stripAspasExternas()` antes de re-escapar não é necessário (delegação direta); (b) **DECIMAL** → `(SqlEscape.aspas(resultado.literalSql()), "NULL")` — `Coercionador.literalSql()` para DECIMAL retorna o número sem aspas (ex: `1234.56`); aplicar `SqlEscape.aspas` envelopa em aspas porque coluna é `varchar(250)`; (c) **DATA** → `(SqlEscape.aspas(resultado.literalSql().replaceFirst("^DATE ", "").replaceAll("'", "")), "NULL")` — `Coercionador.literalSql()` para DATA retorna `DATE '2024-01-15'`; helper extrai a porção `'YYYY-MM-DD'` literal pura (sem prefixo `DATE`) e re-envelopa; (d) **MULTIPLA_ESCOLHA** → `("''", resultado.literalSql())` — `valor` literal vazio (string `''`) e `literalSql()` para alternativa retorna o id numérico que vai direto em `idalternativa`. **Decisão arquitetural** (ver Trade-offs §6): pós-processar `Coercionador.literalSql()` em vez de adicionar API nova ao `Coercionador` preserva o contrato existente (consumido por 4.2/colunas tipadas das tabelas principais) e isola a lógica específica da tabela de respostas dentro de `SqlGeradorUpsert`. |
| AC12 | **Cobertura JaCoCo ≥ 80 % linhas** em `SqlGeradorUpsert` e `ResultadoUpsert` (módulos críticos NFR-09 — meta local ≥ 80 % com folga sobre piso global de 70 %). Verificação por inspeção de `target/site/jacoco/.../index.html`. **Não regredir** cobertura prévia de `Coercionador` (deve permanecer **100 %/100 %**), `SqlEscape` (100 %), `LinhaMapeada`, `ResultadoUpdate`, `SqlGeradorUpdate`, `Fluxo` — todos da 4.2. |
| AC13 | **Mensagens 100 % em PT** (NFR-01). `org.jboss.logging.Logger` **não** é usado em `SqlGeradorUpsert`/`ResultadoUpsert` — função pura, falha vai como dado em `ResultadoUpsert.erros`. Sem `System.out.println` (CONVENCOES.md). Imports absolutos (proibido `import ...geo.*`); pacotes lowercase. |
| AC14 | **`./mvnw -B verify` verde** sem novas dependências em `pom.xml` (JDK + classes existentes). Sem alterações em `src/main/resources/META-INF/native-image/reflect-config.json` (nenhuma classe nova é serializada por Jackson nem acessada por reflexão; `ResultadoUpsert` é construído em código pelo `SqlGeradorUpsert`, não desserializado de JSON). Suíte total esperada: baseline atual **285 + ~30–40 novos testes** (≈ 315–325). |
| AC15 | **Smoke programático de não-regressão de formato.** Teste dedicado parametrizado que, para uma matriz de cenários (happy 4 tipos / múltiplos campos / O'Brien no código / vazios pulados) × `@EnumSource(Fluxo.class)`, asserta que cada SQL emitido (a) inicia com `"INSERT INTO " + fluxo.tabelaRespostas()`, (b) contém `"ON CONFLICT (referencia, idcampo) DO UPDATE SET"` e (c) contém `"EXCLUDED.valortexto"`, `"EXCLUDED.valornumerico"`, `"EXCLUDED.valordata"`, `"EXCLUDED.idalternativa"`. Garantia bloqueante contra mudança acidental no formato. |
| AC16 | **Pré-requisito P0 dentro do escopo desta story**: adicionar método `public String sequenceRespostas()` em `Fluxo` (paralelo a `tabelaRespostas()`/`colunaChave()`), retornando `"s_respostaterreno_id"` para TERRITORIAL e `"s_respostasegmento_id"` para PREDIAL. Teste em `FluxoTest` cobre os 2 valores. Justificativa: trivial (~5 linhas + teste), evita criar story DDL separada para um único método de domínio, mantém `SqlGeradorUpsert` desacoplado de string mágica. |

## Tasks / Subtasks

- [ ] **T1** — Criar `geracao/ResultadoUpsert` (record VO) — Cobre **AC1, AC8, AC9**.
  - Criar `src/main/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpsert.java`:
    - `public record ResultadoUpsert(List<String> sqls, List<String> erros)`.
    - Construtor canônico aplica `List.copyOf(sqls)` e `List.copyOf(erros)` (`null` → `List.of()`).
    - Fábricas:
      - `public static ResultadoUpsert sucesso(List<String> sqls)` — valida `sqls != null` e nenhum elemento `null`/blank ou `IllegalArgumentException` PT; chama `new ResultadoUpsert(sqls, List.of())`. Aceita lista vazia (caso degenerado AC8).
      - `public static ResultadoUpsert falha(List<String> erros)` — valida `erros != null && !erros.isEmpty()` e nenhum elemento `null`/blank ou `IllegalArgumentException` PT; chama `new ResultadoUpsert(List.of(), erros)`.
    - Método `public boolean ok()` retornando `erros.isEmpty()`.
    - Javadoc cita Story 4.3, paralelo com `ResultadoUpdate` (4.2) e contraste explícito sobre lista de SQLs (uma linha gera N UPSERTs).

- [ ] **T2** — Criar esqueleto de `geracao/SqlGeradorUpsert` (validação de args + iteração) — Cobre **AC2, AC3**.
  - Criar `src/main/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpsert.java`:
    - `public final class SqlGeradorUpsert` com construtor público sem argumentos.
    - Método `public ResultadoUpsert gerar(LinhaMapeada linha, Mapeamento mapeamento, Fluxo fluxo, Coercionador coercionador)`.
    - Validar args não-`null` com `if (x == null) throw new IllegalArgumentException("<x> não pode ser nulo.")` em PT (espelha Story 4.2 — AC5 contrata `IllegalArgumentException`, não `NullPointerException`).
    - Iterar `mapeamento.colunasDinamicas().entrySet()`. Para cada `(header, coluna)`:
      - Pular se `coluna.status() != StatusMapeamento.MAPEADO`.
      - Pular se `linha.celulasDinamicas().get(header)` é `null` ou `String.isBlank()`.
      - Senão, encaminhar para coerção (T3).

- [ ] **T3** — Implementar lógica de coerção tipada + acumulação de erros — Cobre **AC4, AC5, AC9**.
  - Para cada célula dinâmica processada, chamar `coercionador.coagir(valor, coluna.tipo(), coluna.alternativas())`.
  - Acumular falhas em `List<String> erros` no formato `"Coluna '" + header + "': " + rc.erro()`.
  - Coagir `linha.codigoImovel()` como `Tipo.TEXTO` (chave de `referencia`); falha vira `"Código do imóvel: " + rc.erro()` na mesma lista.
  - Se `!erros.isEmpty()` ao final do passo de coerção → `return ResultadoUpsert.falha(erros);`.

- [ ] **T4** — Implementar formatação SQL determinística por célula — Cobre **AC6, AC7, AC10, AC11**.
  - Helper privado `private static String colunaFisicaPorTipo(Tipo t)` com `switch` exaustivo:
    - `TEXTO → "valortexto"`, `DECIMAL → "valornumerico"`, `DATA → "valordata"`, `MULTIPLA_ESCOLHA → "idalternativa"`.
  - Para cada célula coagida com sucesso, montar SQL determinístico em **uma linha** com 4 placeholders de valor (3 sempre `NULL`, 1 com o literal coagido):
    ```
    INSERT INTO <fluxo.tabelaRespostas()> (referencia, idcampo, valortexto, valornumerico, valordata, idalternativa) VALUES (<literalChave>, <coluna.idcampo()>, <vT>, <vN>, <vD>, <vIA>) ON CONFLICT (referencia, idcampo) DO UPDATE SET valortexto = EXCLUDED.valortexto, valornumerico = EXCLUDED.valornumerico, valordata = EXCLUDED.valordata, idalternativa = EXCLUDED.idalternativa;
    ```
  - Adicionar à `List<String> sqls` na ordem de iteração de `colunasDinamicas()` (AC7).
  - Escape de `referencia` é responsabilidade transparente do `Coercionador.coagirTexto` (delega a `SqlEscape.aspas`) — AC10.
  - Escape de TEXTO de células dinâmicas idem — AC11.

- [ ] **T5** — Caso degenerado e retorno de sucesso — Cobre **AC8**.
  - Se ao final da iteração `sqls.isEmpty()` (mapeamento sem dinâmicas, ou todas pendentes/vazias) **e** `erros.isEmpty()` → `return ResultadoUpsert.sucesso(List.of());`.
  - Caso normal de sucesso → `return ResultadoUpsert.sucesso(sqls);`.

- [ ] **T6** — Testes unitários — Cobre **AC2, AC3, AC4, AC5, AC6, AC7, AC8, AC9, AC10, AC11, AC15**.
  - **Framework**: JUnit 5 puro (sem AssertJ — alinhado a `CONVENCOES.md` e ao padrão das Stories 4.1/4.2).
  - **Localização**:
    - `src/test/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpsertTest.java` — ~9 testes:
      - `sucesso(List.of("..."))` happy.
      - `sucesso(List.of())` aceita lista vazia (caso degenerado AC8).
      - `sucesso(null)` falha PT.
      - `sucesso` com elemento blank/null falha PT.
      - `falha(List.of("erro PT"))` happy.
      - `falha(null)`/`falha(emptyList)` falham PT.
      - `falha` com elemento blank/null falha PT.
      - `ok()` true/false.
      - Imutabilidade da lista de SQLs e da lista de erros (tentativa de mutação externa não afeta o record).
    - `src/test/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpsertTest.java` — ~22 testes em **`@Nested` por AC** (padrão da 4.2):
      - **`ValidacaoDeArgumentos`** (AC2): 4 testes — cada arg `null` → `IllegalArgumentException` com mensagem PT específica.
      - **`FiltragemDeCelulas`** (AC3): 3 testes — coluna PENDENTE pulada; célula `null` pulada; célula `"  "` (whitespace) pulada.
      - **`CoercaoPorTipo`** (AC4 + AC6): 4 testes — TEXTO popula `valortexto` e demais NULL; DECIMAL popula `valornumerico`; DATA popula `valordata` (formato `DATE 'yyyy-MM-dd'`); MULTIPLA_ESCOLHA popula `idalternativa` com id numérico.
      - **`OrdemDeterministica`** (AC7): 1 teste — mapeamento com 3 colunas dinâmicas (LinkedHashMap), asserção da ordem dos UPSERTs em `sqls()`.
      - **`AcumulacaoDeErros`** (AC5 + AC9 — fecha ISSUE-4.2-02): 2 testes — (a) 1 falha de coerção (DECIMAL inválido) → `falha(["Coluna 'AREA': valor 'XYZ' não é decimal válido"])` e nenhum SQL; (b) 2 falhas simultâneas (DECIMAL inválido + DATA inválida) → lista com **ambas** mensagens, ordem preservada.
      - **`CasoDegenerado`** (AC8): 2 testes parametrizados `@EnumSource(Fluxo.class)` — (a) `colunasDinamicas()` vazio → `sucesso(List.of())`; (b) todas dinâmicas vazias/pendentes → `sucesso(List.of())`. Contraste documentado vs. 4.2 (que retornava `falha`).
      - **`EscapeNoReferencia`** (AC10): 2 testes — `O'Brien` no código → `VALUES ('O''Brien', ...)`; `1'; DROP TABLE x; --` neutralizado.
      - **`EscapeEmValorTexto`** (AC11): 1 teste — célula TEXTO com aspa simples vira literal inerte em `valortexto`.
      - **`SmokeFormatoNaoRegressao`** (AC15): teste parametrizado `@EnumSource(Fluxo.class)` × matriz de cenários, assertando `startsWith("INSERT INTO " + fluxo.tabelaRespostas())`, `contains("ON CONFLICT (referencia, idcampo) DO UPDATE SET")` e os 4 `EXCLUDED.<coluna>`.
      - **`AmbosFluxos`** (integração com `Fluxo.tabelaRespostas()`): cada cenário smoke roda para `TERRITORIAL` e `PREDIAL`.
  - Helpers no teste: `mapeamentoComDinamicas(Fluxo, Map<String, ColunaDinamica>)` usando `LinkedHashMap` explícito (premissa documentada do AC7); `colunaMapeada(int idcampo, Tipo, Map alternativas)` para reduzir boilerplate.

- [ ] **T7** — Verificar cobertura JaCoCo (AC12).
  - Rodar `./mvnw -B verify` e abrir `target/site/jacoco/`:
    - `br.com.arxcode.tematica.geo.geracao/SqlGeradorUpsert.html` ≥ 80 % linhas (esperado ≥ 90 %).
    - `br.com.arxcode.tematica.geo.geracao/ResultadoUpsert.html` ≥ 80 % (esperado 100 %).
    - **Sem regressão**: `Coercionador` 100 %/100 %; `SqlEscape` 100 %; `SqlGeradorUpdate` 90 %/85 %; `ResultadoUpdate`, `LinhaMapeada` 100 %/100 %; `Fluxo` 100 %.
  - Anotar valores no File List.

- [ ] **T8** — `./mvnw -B verify` final verde + atualização da story — Cobre **AC13, AC14**.
  - Suíte: baseline **285 → ~315–325 testes** (delta esperado **+30–40**, sem skips/erros).
  - Atualizar Tasks marcadas `[x]`, File List com cobertura, Completion Notes e Change Log v1.x.
  - Sem novas deps em `pom.xml`; sem alteração em `reflect-config.json`.

## Dev Notes

### Decisões arquiteturais

- **Função pura, sem CDI** — `SqlGeradorUpsert` segue o mesmo padrão estabelecido por `Coercionador` (Story 4.1 AC8) e `SqlGeradorUpdate` (Story 4.2 AC5): `public final class`, construtor público sem argumentos, sem `@ApplicationScoped`. Razões: (a) determinístico, sem efeitos colaterais; (b) testabilidade com JUnit puro; (c) consumidor (Story 4.5) decide se instancia uma vez ou cria por chamada. **`Coercionador` é injetado por parâmetro** em vez de campo `final` no construtor — evita acoplar a fábrica do `SqlGeradorUpsert` à do `Coercionador`. [Source: `src/main/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpdate.java:57-97`]
- **`ResultadoUpsert` em vez de `ResultadoUpdate` reaproveitado** — embora a forma binária "sucesso vs. falha" seja idêntica, o slot de sucesso muda de `String sql` para `List<String> sqls` (uma linha planilha gera **N** UPSERTs, um por célula dinâmica). Reusar `ResultadoUpdate` exigiria sentinela ou serialização (UPSERT por `\n`) — ambas pioram a API. Novo VO específico mantém o tipo de retorno limpo e o paralelo arquitetural óbvio. [Source: `src/main/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpdate.java:31-69`]
- **Caso degenerado tratado como `sucesso(List.of())` — divergência intencional vs. Story 4.2** — na 4.2, `colunasFixas()` vazio → `falha("Linha sem colunas fixas a atualizar")` porque atualizar nada na tabela principal seria erro de mapeamento. Aqui, **não ter dinâmicas é situação válida** (uma planilha pode legitimamente ter 0 colunas dinâmicas mapeadas, ou todas as células dinâmicas de uma linha podem estar vazias — operador não preencheu). O orquestrador da Story 4.5 ainda chamará `SqlGeradorUpdate` para a mesma linha; portanto retornar `sucesso(List.of())` aqui não faz a linha "sumir". Documentado no Javadoc com link de contraste para a 4.2.
- **Célula dinâmica vazia → pular (não emitir UPSERT com tudo `NULL`)** — divergência intencional vs. Story 4.2 (lá `null` vira `NULL` literal no `SET col = NULL`, expressando intenção de zerar). Aqui pular é correto porque: (a) tabela de respostas é UPSERT por `(referencia, idcampo)` — emitir linha com tudo NULL adiciona registro vazio sem semântica; (b) operador deixar célula em branco signfica "não respondi", não "respondi NULL" (informação ausente vs. ausência informativa); (c) reduz volume do `.sql` proporcionalmente ao preenchimento real. Decisão documentada no Javadoc do método `gerar`.
- **Filtragem por `status == MAPEADO`** — `ColunaDinamica` aceita 3 status (MAPEADO/PENDENTE/IGNORADO — Story 3.3); apenas MAPEADO tem `idcampo != null` e `tipo != null` garantidos (Story 3.3 valida isso antes do `mapping.json` chegar aqui). Iteração defensiva pula PENDENTE/IGNORADO sem falhar — a Story 4.5 (orquestrador) é quem decide se mapping com pendências sequer chega ao `importar` (provável `validar` bloquear antes). Aqui é defesa em profundidade. [Source: `src/main/java/br/com/arxcode/tematica/geo/mapeamento/ColunaDinamica.java:33-45`]
- **Helper `colunaFisicaPorTipo(Tipo)` privado em `SqlGeradorUpsert` em vez de método em `Tipo`** — `Tipo` é enum de domínio puro (4 valores literais espelhando `campo.tipo` do banco; `Tipo.java:15-19`). Adicionar `colunaValorRespostas()` ao enum acoplaria detalhe de persistência das tabelas `respostaterreno`/`respostasegmento` ao domínio (violação de SRP). O helper privado mantém o acoplamento contido na camada `geracao/` que já conhece a forma do SQL. Trade-off: se um dia surgir outro consumidor da mesma tabela física por tipo, promover para utilitário compartilhado em `geracao/`.
- **Sintaxe `INSERT ... ON CONFLICT ... DO UPDATE` é PostgreSQL-específica (CON-04)** — alvo é PG (CON-04 / `application.properties` aponta para `quarkus-jdbc-postgresql`). Documentado no Javadoc de `SqlGeradorUpsert`. Sintaxe alternativa `MERGE` (PG ≥ 15) descartada porque (a) escopo da prefeitura ainda usa PG ≥ 9.5 (suporte a `ON CONFLICT`); (b) `EXCLUDED.coluna` na cláusula `DO UPDATE SET` é sintaxe estável e amplamente conhecida.
- **Apenas a coluna física correspondente ao `Tipo` é populada; demais ficam `NULL`** — esquema da tabela de respostas mantém 4 slots (texto/numérico/data/idalternativa) e popula apenas o relevante. Mesma estratégia do `DO UPDATE SET` (atualiza os 4 com `EXCLUDED`, mas 3 deles serão NULL → preserva a coluna correta no UPDATE também). Decisão simplifica o gerador (forma única de SQL) à custa de 3 NULLs por UPSERT (custo aceitável; consistente com schema atual).
- **Acumulação plena de erros (AC5/AC9) fecha ISSUE-4.2-02** — na 4.2, AC9 só pôde ser exercitado documentalmente (TEXTO não falha para entradas non-blank — `Coercionador.java:80-89,93-96`). Aqui, `DECIMAL`/`DATA`/`MULTIPLA_ESCOLHA` falham para entradas inválidas (`Coercionador.java:108-113,116-132,134-153`), permitindo teste real com 2+ falhas simultâneas. Anotado em ISSUE-4.2-02 (gate 4.2) e fechado nesta story.

### Pontos verificados na arquitetura/PRD

- **FR-12** (UPSERT com `INSERT ... ON CONFLICT (referencia, idcampo) DO UPDATE`). [Source: `docs/prd/prd.md:95`]
- **FR-13** (coerção de valores conforme `campo.tipo`; erro registrado no `.log`; pipeline continua). [Source: `docs/prd/prd.md:96`]
- **CON-04** (PostgreSQL-only — `INSERT ... ON CONFLICT` é sintaxe PG-específica). [Source: `docs/prd/prd.md:127`]
- **NFR-09** (cobertura ≥ 70 % em módulo crítico — geração de SQL é nominal). [Source: `docs/prd/prd.md:117`]
- **NFR-01** (mensagens em PT). [Source: `docs/prd/prd.md:109`]
- **Mapa de fluxos** — TERRITORIAL: `tribcadastroimobiliario.tribcadastrogeral_idkey = respostaterreno.referencia`; PREDIAL: `tribimobiliariosegmento.idkey = respostasegmento.referencia`. [Source: `docs/architecture/arquitetura.md:16-17`]
- **Invariante "respostaterreno/respostasegmento UPSERT por (referencia, idcampo)"** — regra invariável do domínio. [Source: `AGENTS.md` §Domínio, "Regras invioláveis"]
- **`Fluxo.tabelaRespostas()` já existe** — `TERRITORIAL → "respostaterreno"`; `PREDIAL → "respostasegmento"`. [Source: `src/main/java/br/com/arxcode/tematica/geo/dominio/Fluxo.java:42,49,86-88`]
- **`Tipo` enum** — 4 valores `TEXTO`, `DECIMAL`, `DATA`, `MULTIPLA_ESCOLHA`. [Source: `src/main/java/br/com/arxcode/tematica/geo/dominio/Tipo.java:15-19`]
- **`Mapeamento.colunasDinamicas()` retorna `Map<String, ColunaDinamica>`** — chave é o header da planilha. [Source: `src/main/java/br/com/arxcode/tematica/geo/mapeamento/Mapeamento.java:32-37`]
- **`ColunaDinamica` campos disponíveis**: `status`, `idcampo` (Integer boxed), `tipo`, `alternativas` (Map\<String, Integer\>), `motivo`, `sugestoes`. **Atenção**: nome real do campo é `alternativas` (não `alternativasMap` como o briefing inicial sugeria). [Source: `src/main/java/br/com/arxcode/tematica/geo/mapeamento/ColunaDinamica.java:33-39`]
- **`Coercionador.coagir(valor, tipo, alternativas)`** — API estável; aceita `null` em `alternativas` para tipos `!= MULTIPLA_ESCOLHA`; devolve `ResultadoCoercao.literalSql()` pronto para concatenação (com aspas para TEXTO/DATA, sem aspas para DECIMAL/idAlternativa). [Source: `src/main/java/br/com/arxcode/tematica/geo/geracao/Coercionador.java:79-89`]
- **`Coercionador` para MULTIPLA_ESCOLHA** retorna o `idAlternativa` como string numérica via `String.valueOf(id)` (sem aspas) — **diretamente concatenável** na coluna `idalternativa` (que é `integer`). [Source: `src/main/java/br/com/arxcode/tematica/geo/geracao/Coercionador.java:149`]
- **`LinhaMapeada.celulasDinamicas()`** — `Map<String, String>` indexado pelo mesmo header que `Mapeamento.colunasDinamicas()`; ordem preservada via `LinkedHashMap` imutável. [Source: `src/main/java/br/com/arxcode/tematica/geo/mapeamento/LinhaMapeada.java:48-63`]
- **`SqlEscape.aspas(s)`** — utilitário estático para escape de aspas simples (já fechou ISSUE-4.1-02 na 4.2); transparente nesta story (delegado pelo `Coercionador`). [Source: `src/main/java/br/com/arxcode/tematica/geo/geracao/SqlEscape.java:44-46`]
- **Padrão arquitetural de função pura `final class`** consolidado pela 4.2. [Source: `src/main/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpdate.java:57-150`]
- **ISSUE-4.2-02 (LOW, owner @dev)** — cobertura plena do ramo de acumulação de erros (AC9 da 4.2) será exercitada naturalmente na Story 4.3 (`Tipo != TEXTO` falha). [Source: `docs/qa/gates/4.2-sql-gerador-update.yml`]

### Fallbacks de arquitetura

- `tech-stack.md` / `coding-standards.md` / `source-tree.md` ausentes. Fonte canônica de convenções: `CONVENCOES.md` + `AGENTS.md`. Fonte canônica de estrutura: `docs/architecture/arquitetura.md` §8. [Note: igual às Stories 4.1 e 4.2.]

### Suggested Files

- **MODIFICAR**: `src/main/java/br/com/arxcode/tematica/geo/dominio/Fluxo.java` (adicionar `sequenceRespostas()`).
- **MODIFICAR**: `src/test/java/br/com/arxcode/tematica/geo/dominio/FluxoTest.java` (cobrir nova API).

**Novos (produção):**
- `src/main/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpsert.java`
- `src/main/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpsert.java`

**Modificar (produção):**
- _(nenhum)_ — todos os pré-requisitos foram entregues pelas Stories 4.1 e 4.2.

**Novos (testes):**
- `src/test/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpsertTest.java`
- `src/test/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpsertTest.java`

**Modificar (testes):**
- _(nenhum)_

**Não tocar:**
- `pom.xml` (sem novas deps).
- `src/main/resources/META-INF/native-image/reflect-config.json` (nenhuma classe nova é serializada por Jackson nem acessada por reflexão; `ResultadoUpsert` é construído em código pelo `SqlGeradorUpsert`).
- `Tipo.java`, `Fluxo.java`, `Mapeamento.java`, `ColunaDinamica.java`, `LinhaMapeada.java`, `Coercionador.java`, `ResultadoCoercao.java`, `SqlEscape.java`, `ResultadoUpdate.java`, `SqlGeradorUpdate.java` (consumidos como contratos existentes).
- Stories 3.x, 4.1, 4.2 (não reabrir).

## Out-of-scope desta story

- **Hardening de `Mapeamento.colunasFixas`/`colunasDinamicas` com cópia defensiva via `LinkedHashMap+unmodifiableMap`** — pendência herdada de **ISSUE-4.2-01** (LOW, owner @dev). Risco: caller que construa `Mapeamento` programaticamente com `HashMap` perde ordem. Mitigação: testes desta story usam `LinkedHashMap` explícito (espelha tratamento da VAL-4.2-01). Resolução planejada para janela entre 4.5 e 6.1.
- **Tipagem completa das colunas fixas** (Story **4.2.1**) — débito conhecido **ISSUE-4.2-04**. Decisão @po: refinar/criar 4.2.1 após 4.3/4.4/4.5 entregues, com base em volume real de quebras por cast implícito do PostgreSQL. Esta story (4.3) **não** sofre dessa limitação porque consome `coluna.tipo()` real do `ColunaDinamica` (já tipado).
- **`SqlGerador` que orquestre 4.2 + 4.3 por linha (UPDATE + N UPSERTs)** — Story **4.5** (`ImportarCommand`). Esta story entrega apenas o gerador puro de UPSERT.
- **Logging de falhas em arquivo `.log`** — Story **4.4** (`LogErros`). Aqui apenas devolvemos `erros` no `ResultadoUpsert`.
- **Leitor de planilha que constrói `LinhaMapeada`** — Story **4.5** (consome a Story 2.1 `LeitorPlanilha`).
- **Validação E2E em binário nativo (GraalVM)** — Stories 6.1 / 6.3.
- **Suporte a `executemany`/batch JDBC** — projeto não executa SQL (CON-03), gera arquivo.
- **`MERGE` PG ≥ 15 ou outras sintaxes não-`ON CONFLICT`** — alvo é `INSERT ... ON CONFLICT ... DO UPDATE` por compatibilidade com PG ≥ 9.5 e por ser o padrão amplamente conhecido.

## Trade-offs registrados

- **`ResultadoUpsert` novo VO vs. reuso de `ResultadoUpdate`** — escolhi novo VO porque o slot de sucesso muda de `String` para `List<String>`. Custo: ~70 LoC duplicadas (record + 2 fábricas + Javadoc). Benefício: API limpa, paralelo arquitetural óbvio, sem sentinelas/serialização.
- **Helper `colunaFisicaPorTipo(Tipo)` privado em `SqlGeradorUpsert`** vs. método em `Tipo` — privado preserva pureza do enum de domínio. Custo: se outro consumidor da mesma tabela aparecer, refatoração para utilitário compartilhado. Benefício: SRP respeitado, `Tipo` permanece desacoplado de detalhes de persistência.
- **Célula vazia → pular (4.3) vs. NULL literal (4.2)** — divergência intencional. Custo: regra de processamento diferente entre os dois geradores (operador precisa entender a distinção). Benefício: semântica correta para tabelas de respostas (vazio ≠ resposta NULL).
- **Caso degenerado → sucesso(empty) (4.3) vs. falha (4.2)** — divergência intencional. Custo: orquestrador (4.5) precisa lidar com `List<String>` que pode estar vazia (sem ser falha). Benefício: linha sem dinâmicas é cenário válido e não bloqueia o `UPDATE` da tabela principal.
- **Apenas 1 coluna física populada por UPSERT; demais NULL** — custo: 3 `NULL` literais por UPSERT no `.sql` (custo de I/O insignificante). Benefício: forma única de SQL (1 template), gerador simples.
- **Sintaxe `EXCLUDED.coluna` no `DO UPDATE SET`** — **lock-in PostgreSQL** assumido (CON-04). Risco: portabilidade futura. Mitigação: CON-04 é constraint formal do projeto.
- **Acumular todas as falhas antes de retornar** — custo: O(n) também no caminho de falha (loop completo) em vez de O(1). Benefício: UX do operador (todos os erros de uma só vez) — mesmo trade-off já adotado e validado pela 4.2.

## Testing

- **Framework:** JUnit 5 puro (sem AssertJ — alinhado a `CONVENCOES.md` e ao padrão das Stories 4.1/4.2).
- **Localização:** `src/test/java/br/com/arxcode/tematica/geo/geracao/`.
- **Convenção:** sufixo `Test` (Surefire); evitar `IT` (skipITs=true global).
- **Parametrização:** preferir `@ParameterizedTest` + `@EnumSource(Fluxo.class)` para cobrir TERRITORIAL e PREDIAL com mesmo cenário (especialmente AC15 smoke de formato e AC8 caso degenerado).
- **Fixtures:** literais nos próprios testes — sem I/O. **`LinkedHashMap` explícito** ao construir `Mapeamento.colunasDinamicas()` (pendência ISSUE-4.2-01 herdada — ver Out-of-scope). Helper `colunaMapeada(int idcampo, Tipo, Map alternativas)` para reduzir boilerplate.
- **Cobertura esperada:**
  - `SqlGeradorUpsert` ≥ 90 % linhas (lógica concentrada, ramos finitos: 4 tipos × filtros × erros).
  - `ResultadoUpsert` 100 % (record com 2 fábricas + 1 método).
  - **Sem regressão**: `Coercionador` 100 %/100 %; `SqlEscape` 100 %; `LinhaMapeada` 100 %/100 %; `ResultadoUpdate` 100 %/100 %; `SqlGeradorUpdate` 90 %/85 %; `Fluxo` 100 %.
- **Critério NFR-09** — esta story é módulo crítico (geração de SQL é nominal no PRD).
- **Smoke não-regressão de formato (AC15)** — teste dedicado e **bloqueante**; não pode regredir em stories futuras (qualquer mudança no template de UPSERT exige atualização explícita da asserção).
- **Cenário-chave para fechar ISSUE-4.2-02** — teste em `AcumulacaoDeErros` com 2 falhas simultâneas (`Tipo.DECIMAL` + `Tipo.DATA`) confirma plenamente o ramo de acumulação que a 4.2 só pôde exercitar documentalmente.

## File List

_Preenchido pelo @dev após implementação._

**Novos (produção):**
- `src/main/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpsert.java` — record VO `(List<String> sqls, List<String> erros)` com fábricas `sucesso(...)`/`falha(...)` e `ok()`. Cobertura JaCoCo: _(preencher)_.
- `src/main/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpsert.java` — `public final class` (função pura, sem CDI) com `gerar(LinhaMapeada, Mapeamento, Fluxo, Coercionador) → ResultadoUpsert`. Cobertura JaCoCo: _(preencher)_.

**Novos (testes):**
- `src/test/java/br/com/arxcode/tematica/geo/geracao/ResultadoUpsertTest.java` — _(N testes)_.
- `src/test/java/br/com/arxcode/tematica/geo/geracao/SqlGeradorUpsertTest.java` — _(N testes em @Nested por AC)_.

**Modificados:** _(nenhum esperado)_.

**Não tocados** (conforme planejado): `pom.xml`, `reflect-config.json`, `Tipo.java`, `Fluxo.java`, `Mapeamento.java`, `ColunaDinamica.java`, `LinhaMapeada.java`, `Coercionador.java`, `ResultadoCoercao.java`, `SqlEscape.java`, `ResultadoUpdate.java`, `SqlGeradorUpdate.java`.

**Métricas finais:** _(preencher)_.

## Completion Notes

_Preenchido pelo @dev após implementação._

## Definition of Done (DoD)

- [ ] AC1–AC15 verdes (validados por testes; AC10/AC11/AC15 são bloqueantes de segurança/formato).
- [ ] `./mvnw -B verify` sem falhas (BUILD SUCCESS).
- [ ] Cobertura JaCoCo `SqlGeradorUpsert` ≥ 80 % linhas; `ResultadoUpsert` ≥ 80 % linhas (anotada no File List).
- [ ] **Regressão zero** em cobertura JaCoCo de `Coercionador` (100 %/100 %), `SqlEscape` (100 %), `LinhaMapeada`, `ResultadoUpdate`, `SqlGeradorUpdate`, `Fluxo` — todos da 4.2.
- [ ] Javadoc PT presente nas 2 classes novas (incluindo referência cruzada a `ResultadoUpdate` e `SqlGeradorUpdate` para contraste; menção explícita a CON-04 / `EXCLUDED` / sintaxe PG).
- [ ] File List preenchida pelo @dev (incluindo contagem de testes e % de cobertura).
- [ ] Completion Notes registra: tratamento da ISSUE-4.2-02 (caminho de acumulação de erros plenamente exercitado), decisões de implementação tomadas, e qualquer desvio do plano.
- [ ] Change Log atualizado com versão da implementação (@dev) e da revisão (@qa).
- [ ] `@po *close-story 4.3`.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-04-22 | 0.1 | Stub inicial (AC1–AC7 esqueléticos, 3 tasks vagas, sem Dev Notes detalhado, sem Out-of-scope/Suggested Files/Testing detalhado, estimativa 1 h, dependência apenas da 4.1, AC5 propunha idempotência via Testcontainers — fora do espírito CON-03 que gera arquivo `.sql` sem executar). | @sm |
| 2026-04-30 | 1.0 | **Refino completo no padrão Story 4.1 v1.0 / Story 4.2 v1.0.** Reusa pré-requisitos entregues pela 4.2 (`SqlEscape`, `LinhaMapeada`, `Fluxo.colunaChave`/`tabelaRespostas`, padrão arquitetural função pura `final class`, `Coercionador` injetado por parâmetro). **Pré-requisito novo único**: `ResultadoUpsert` (record VO em `geracao/`, análogo a `ResultadoUpdate` mas com slot de sucesso `List<String> sqls` em vez de `String sql` — uma linha planilha gera N UPSERTs). **Decisões de design**: (a) **caso degenerado retorna `sucesso(List.of())`** (divergência intencional vs. 4.2 que retornava `falha`) — linha sem dinâmicas é cenário válido (operador pode legitimamente deixar tudo em branco; o orquestrador 4.5 ainda gera UPDATE da principal); (b) **célula dinâmica vazia → pular** (divergência intencional vs. 4.2 que emite `coluna = NULL`) — semântica correta para tabela de respostas (vazio = "não respondi", não "respondi NULL"); (c) **helper `colunaFisicaPorTipo(Tipo)` privado em `SqlGeradorUpsert`** em vez de método em `Tipo` — preserva pureza do enum de domínio (SRP); (d) **apenas a coluna física correspondente ao Tipo é populada; demais 3 ficam NULL** — forma única de SQL, gerador simples; (e) **sintaxe `INSERT ... ON CONFLICT ... DO UPDATE SET ... = EXCLUDED.*`** documentada como PG-específica (CON-04). **Política de erro**: falha em qualquer coerção de célula dinâmica → abandona todos os UPSERTs da linha, acumula todas as falhas para reporte único (espelha 4.2 AC9; alinhada FR-13 e AGENTS.md). **Fecha ISSUE-4.2-02**: AC9 plenamente exercitável agora com `Tipo != TEXTO` (DECIMAL/DATA falham para entradas inválidas — `Coercionador.java:108-113,116-132`); teste em `AcumulacaoDeErros` força 2 falhas simultâneas. **Smoke CON-03 reforçado** (AC10/AC11): escape no `referencia` (vetor `O'Brien` + injection) e em valores TEXTO de células dinâmicas — delegação transparente ao `SqlEscape.aspas` via `Coercionador.coagirTexto`. **Smoke não-regressão de formato** (AC15): asserção programática que cada SQL inicia com `INSERT INTO <tabelaRespostas>`, contém `ON CONFLICT (referencia, idcampo) DO UPDATE SET` e os 4 `EXCLUDED.<coluna>`. AC1–AC15 verificáveis com critérios objetivos (`assertEquals` da string SQL exata, asserções programáticas sobre `startsWith`/`contains`). Tasks T1–T8 com mapeamento Task→AC explícito ("Cobre AC#"). Dev Notes registra 14 fontes citadas com `[Source: file:line]` (excede mínimo de 8); Out-of-scope referencia ISSUE-4.2-01 (hardening Mapeamento) e ISSUE-4.2-04 (Story 4.2.1 tipagem fixas) como pendências herdadas; Trade-offs registra 7 decisões com justificativa. **Estimativa ajustada de 1 h → 2 h** — menor que a 2,5 h da 4.2 porque todos os 4 pré-requisitos já estão entregues (`SqlEscape`, `LinhaMapeada`, `Coercionador`, `Fluxo.colunaChave`/`tabelaRespostas`); só precisa criar 2 classes novas (`ResultadoUpsert` + `SqlGeradorUpsert`) sem refatorações transversais. Pronta para `@po *validate-story-draft 4.3`. | River (@sm) |
| 2026-04-30 | 1.1 | **Validação de draft (Pax @po) — CONDITIONAL GO, score 9/10. Status: Draft → Approved.** Aderência ao template 4.1/4.2 verificada (11 seções obrigatórias presentes; densidade equivalente). Qualidade dos 15 ACs verificada — todos objetivamente verificáveis (assertEquals exato em SQL, JaCoCo ≥ 80 %, IllegalArgumentException PT, asserções `startsWith`/`contains`). Mapeamento Task→AC explícito em T1–T8 (cobertura completa: cada AC aparece em ≥ 1 Task). **Spot-check de fontes (8 verificações)**: `Fluxo.tabelaRespostas()` linhas 86-88 ✅, `Fluxo.colunaChave()` 100-102 ✅, `Tipo` 4 valores TEXTO/DECIMAL/DATA/MULTIPLA_ESCOLHA linhas 15-19 ✅, `Mapeamento.colunasDinamicas()` retorna `Map<String, ColunaDinamica>` linha 37 ✅, `ColunaDinamica` campos `status/idcampo/tipo/alternativas` linhas 33-39 ✅ (com correção do briefing já registrada na própria story: `alternativas` não `alternativasMap`), `Coercionador.coagir(valor, tipo, alternativas)` linha 79 e ramos de falha DECIMAL/DATA/MULTIPLA_ESCOLHA confirmados ✅, `SqlEscape.aspas(String)` linha 44 ✅, `ResultadoUpdate` padrão (sucesso/falha/copyOf/ok) confirmado como base do paralelo ✅. **Coerência arquitetural**: `public final class` sem CDI + `Coercionador` injetado por parâmetro + record com `List.copyOf` + erro como dado em `ResultadoUpsert.falha(List<String>)` + mensagens PT + imports absolutos — todos consistentes com 4.1/4.2. **Decisões críticas validadas**: (a) célula vazia → skip vs. 4.2 NULL literal — divergência intencional bem justificada (semântica "não respondi" vs. "respondi NULL", evita poluir tabela de respostas); (b) sintaxe ON CONFLICT EXCLUDED.* — corretamente citada como PG-específica (CON-04, PRD §7); (c) caso degenerado `sucesso(List.of())` vs. 4.2 `falha` — divergência intencional documentada com referência cruzada no Javadoc; (d) helper `colunaFisicaPorTipo(Tipo)` privado em `SqlGeradorUpsert` — correto (preserva pureza do enum); (e) fechamento de ISSUE-4.2-02 confirmado (DECIMAL/DATA falham em entradas inválidas — verificado em `Coercionador.java:108-113,116-132`); (f) estimativa 2 h razoável (todos os 4 pré-requisitos prontos; menor que 4.2 que entregou em ~2,5 h com refatorações transversais). **Out-of-scope correto**: ISSUE-4.2-01 (hardening Mapeamento), ISSUE-4.2-04/Story 4.2.1 (tipagem fixas), Stories 4.4 (LogErros)/4.5 (ImportarCommand)/4.6 mencionadas. **NFR-01 (PT)**: toda a story em português; inglês apenas em literais de código/SQL. **2 issues registradas como deferred-during-dev** (não bloqueiam aprovação): ISSUE-4.3-01 MEDIUM (StatusMapeamento real tem só 2 valores MAPEADO/PENDENTE — não existe IGNORADO; AC3, T2 e Dev Notes linha 128 mencionam "PENDENTE/IGNORADO" — @dev deve simplesmente filtrar `status() == MAPEADO` sem mencionar IGNORADO no código/Javadoc; teste correspondente em `FiltragemDeCelulas` cobre só PENDENTE), ISSUE-4.3-02 LOW (nomes das 4 colunas físicas da tabela de respostas — `valortexto`/`valornumerico`/`valordata`/`idalternativa` — não foram verificados em fonte explícita do repo; arquitetura.md §1 cobre só os joins; @dev deve confirmar via `\d respostaterreno` no Postgres local antes de finalizar T4 — se nomes divergirem, ajustar AC6 + T4 + helper `colunaFisicaPorTipo` em consenso com @sm). **1 INFO**: pequena inconsistência interna entre AC9 (`Coercionador.java:108-109,124,130,136,147,152`) e Dev Notes (`Coercionador.java:108-113,116-132,134-153`) — ambos os ranges são plausíveis e cobrem ramos de falha; sem ação requerida. **Próximo passo**: `@dev *develop 4.3`. | Pax (@po) |
| 2026-04-30 | 2.1 | **Re-validação HIGH (Pax @po) — NO-GO, score 6/10. Status: permanece Draft.** Edits cirúrgicos da v2.0 não propagaram para todas as seções, gerando contradições internas que bloqueiam implementação. **Issues HIGH bloqueantes (4)**: (1) **AC11(a) TEXTO double-escape confirmado** — `Coercionador.coagirTexto` (linha 95) retorna `"'" + SqlEscape.aspas(t) + "'"`, ou seja `literalSql()` para TEXTO **já vem com aspas externas e internas duplicadas** (ex.: `'O''Brien'`). Aplicar `SqlEscape.aspas(resultado.literalSql())` em cima produz `''O''''Brien''` — string SQL corrompida. AC11(a) declara "delegação direta" sem perceber o double-escape. (2) **AC11(b)/AC11(c) confusão sobre contrato de `SqlEscape.aspas`** — AC11(b) afirma que `aspas()` "envelopa em aspas" e AC11(c) idem para DATA. Falso: `SqlEscape.aspas` (linhas 44-46) **apenas duplica `'` interno, não envelopa**. Para DECIMAL `1234.56`, `aspas("1234.56")` devolve `"1234.56"` sem aspas — concatenação geraria `valor=1234.56` (não-string para varchar). Helper `serializarValor` precisa ser `"'" + SqlEscape.aspas(rawSemAspas) + "'"` (mesmo padrão de `Coercionador.coagirTexto:95`). (3) **AC15 totalmente desatualizado** — ainda asserta `"INSERT INTO " + fluxo.tabelaRespostas()` (sem schema `aise.`), `"ON CONFLICT (referencia, idcampo) DO UPDATE SET"` e `EXCLUDED.valortexto/valornumerico/valordata/idalternativa`. Contradiz frontalmente AC6 v2.0 (DELETE+INSERT, schema explícito, coluna única `valor`). Briefing afirmava AC15 atualizado — **não foi**. (4) **T4 totalmente desatualizado** — ainda manda emitir SQL `INSERT ... ON CONFLICT (referencia, idcampo) DO UPDATE SET valortexto = EXCLUDED.valortexto, ...` e referencia helper privado `colunaFisicaPorTipo(Tipo)` (removido na v2.0 em favor de `serializarValor`). T4 contradiz AC6 e AC11 v2.0. **Issues MEDIUM (3)**: (5) **T6 testes desatualizados** — `CoercaoPorTipo` ainda diz "TEXTO popula `valortexto`, DECIMAL popula `valornumerico`, DATA popula `valordata`"; `SmokeFormatoNaoRegressao` ainda asserta `ON CONFLICT` e `EXCLUDED.<coluna>`. (6) **Dev Notes §Decisões arquiteturais desatualizado** — bullets sobre "sintaxe `INSERT ... ON CONFLICT ... DO UPDATE`" (linha 131), "apenas a coluna física correspondente ao Tipo é populada" (linha 132) e helper `colunaFisicaPorTipo` (linha 130) descrevem v1.x. (7) **Dev Notes §Pontos verificados linha 137** — "FR-12 (UPSERT com `INSERT ... ON CONFLICT (referencia, idcampo) DO UPDATE`)" desatualizado; reconciliar FR-12 com estratégia DELETE+INSERT. **Issues LOW (3)**: (8) **Header linha 11** ainda lista "CON-04 (sintaxe `INSERT ... ON CONFLICT ... DO UPDATE` é PG-específica)" — atualizar para citar DELETE+INSERT+sequence. (9) **Trade-offs §6** ainda menciona `EXCLUDED.coluna` lock-in (linha 202) e "Apenas 1 coluna física populada por UPSERT; demais NULL" (linha 201) — substituir por trade-offs DELETE+INSERT (2 statements/célula vs alternativas). Trade-off "Helper `colunaFisicaPorTipo`" (linha 198) deve virar "Helper `serializarValor`". (10) **DoD linha 244** lista "AC1–AC15 verdes" — falta AC16. **Spot-check de fontes (5/5 verificadas no código real)**: `Coercionador.coagirTexto:95` confirma `"'" + SqlEscape.aspas(t) + "'"` (TEXTO já aspeada) ✅; `Coercionador.coagirDecimal:113` `bd.toPlainString()` (DECIMAL nu) ✅; `Coercionador.coagirData:128` `"DATE '" + iso + "'"` (DATA com prefixo) ✅; `Fluxo.tabelaRespostas():86-88` retorna `"respostaterreno"`/`"respostasegmento"` **sem schema** — coerente com AC6 que prepende `aise.` no template ✅; `SqlEscape.aspas:44-46` **apenas duplica `'`, não envelopa** ✅. **AC16 isoladamente OK**: nomes de sequence `s_respostaterreno_id`/`s_respostasegmento_id` coerentes com schema real e prefixo `aise.` aplicado em AC6. **AC6 isoladamente OK**: forma DELETE+INSERT correta, `nextval('aise.<seq>')` com schema explícito (essencial pois SQL é texto exportado). **Mudanças HIGH validadas**: 1.DELETE+INSERT AC6 PASS · 2.Coluna `valor` AC11 **FAIL (double-escape + confusão `aspas`)** · 3.`Fluxo.sequenceRespostas()` AC16 PASS · 4.`nextval('aise.<seq>')` PASS · 5.Coerência Tasks vs ACs **FAIL (T4/T6/AC15 não propagados)** · 6.Change Log v2.0 + Issues PASS. **Score detalhado**: aderência template 1/1 · qualidade ACs (corrupção interna) 0/3 · mapeamento Task→AC (T4/T6 contradizem) 0/2 · spot-check de fontes 2/2 · coerência arquitetural 1/2 · cumpre objetivo da re-validação 2/0 (encontrou bugs HIGH que escaparam à v1.1) → **6/10 NO-GO**. **Próximo passo**: `@sm *draft 4.3 v2.2` para corrigir AC11 (especificar concretamente `"'" + SqlEscape.aspas(rawSemAspas) + "'"` por tipo, com strip de aspas externas em TEXTO), reescrever AC15 (DELETE+INSERT pattern + `aise.<tabela>` + nova asserção sobre `valor=` e `idalternativa=`), reescrever T4 (helper `serializarValor`, 2 statements DELETE+INSERT, `Fluxo.sequenceRespostas()`, `nextval('aise.<seq>')`), atualizar T6 (testes refletindo coluna única `valor`+`idalternativa`), atualizar Dev Notes §Decisões arquiteturais e §Pontos verificados, header CON-04, Trade-offs e DoD AC16. **Issues registradas**: ISSUE-4.3-04 HIGH (double-escape AC11 TEXTO) · ISSUE-4.3-05 HIGH (`SqlEscape.aspas` não envelopa — AC11 DECIMAL/DATA) · ISSUE-4.3-06 HIGH (AC15 contradiz AC6) · ISSUE-4.3-07 HIGH (T4 contradiz AC6/AC11) · ISSUE-4.3-08 MEDIUM (T6 testes desatualizados) · ISSUE-4.3-09 MEDIUM (Dev Notes desatualizado) · ISSUE-4.3-10 LOW (header/Trade-offs/DoD desatualizados). | Pax (@po) |
| 2026-04-30 | 2.0 | **Re-refino HIGH (River @sm) — 3 divergências fundamentais descobertas via inspeção real `\d aise.respostaterreno` no Postgres local. Status: Approved → Draft (requer re-validação @po).** Divergências corrigidas: (1) Schema real tem **coluna única `valor varchar(250)` + `idalternativa`** (não 4 colunas tipadas `valortexto/valornumerico/valordata/idalternativa` como assumido na v1.0/v1.1) — confirmado em `psql \d aise.respostaterreno` 2026-04-30. (2) Índice `(referencia, idcampo)` **não é UNIQUE/PK** (apenas `idx_respostaterreno_campo` btree não-unique) — sintaxe `INSERT ... ON CONFLICT (referencia, idcampo) DO UPDATE` **lançaria erro Postgres** ("there is no unique or exclusion constraint matching the ON CONFLICT specification"). (3) PK isolada `id numeric(10,0) NOT NULL` alimentada por sequence `aise.s_respostaterreno_id` (e `aise.s_respostasegmento_id`) — INSERT precisa fornecer `id` via `nextval(...)`. **Estratégia adotada**: `DELETE FROM resp WHERE referencia=X AND idcampo=Y; INSERT ... VALUES (nextval('aise.s_<tabela>_id'), ...)` — 2 statements/célula. Justificativa: (a) não exige DDL prévio (mantém CON-04); (b) idempotente em re-execução (DELETE tolerante a 0 linhas); (c) padrão clássico pré-PG-9.5 sem perda de garantias; (d) alternativa `UPDATE+INSERT WHERE NOT EXISTS` rejeitada por gerar mesmos 2 statements com sub-SELECT mais complexo; (e) alternativa "criar UNIQUE INDEX em story DDL prévia" rejeitada por hora — registrada em Out-of-scope para futuro. **Evidência empírica de formato dos dados** (amostras coletadas via psql 2026-04-30): DECIMAL → `valor='11418.9505'` (string com 4 casas decimais, `idalternativa=NULL`); TEXTO → `valor`=string livre, `idalternativa=NULL`; MULTIPLA_ESCOLHA → `valor=''` (string vazia, **não NULL**), `idalternativa=<id_FK>`; DATA → zero linhas em `respostaterreno` ainda — assumido formato `'YYYY-MM-DD'` (padrão Postgres), confirmar em fluxo real → **ISSUE-4.3-03 LOW** criada. **Mudanças nesta v2.0**: AC6 reescrito (forma DELETE+INSERT, 2 strings/célula); AC11 reescrito (helper `serializarValor` substituindo `colunaFisicaPorTipo` removido — pós-processa `Coercionador.literalSql()` para strip prefixo `DATE`/wrap em aspas; mantém `Coercionador` intacto para 4.2/colunas tipadas das tabelas principais); AC16 NOVO (pré-req P0 `Fluxo.sequenceRespostas()` ~5 linhas + teste); User Story atualizada; Suggested Files inclui `Fluxo.java`+`FluxoTest.java`; Estimativa 2h → 2,5h (justificada pela complexidade extra: 2 statements/célula, helper `serializarValor` com 4 ramos, pré-req `Fluxo.sequenceRespostas`). **Issues resolvidas**: ISSUE-4.3-02 LOW (validação concluída — schema mapeado, divergência HIGH descoberta e tratada). **Issues criadas**: ISSUE-4.3-03 LOW (formato DATA presumido `'YYYY-MM-DD'` — confirmar empiricamente quando primeiro fluxo real com DATA chegar). **Issues mantidas**: ISSUE-4.3-01 MEDIUM (StatusMapeamento sem IGNORADO — deferred-during-dev). **Out-of-scope reforçado**: criar UNIQUE INDEX em `(referencia, idcampo)` — story DDL futura, descartada por ora. **Próximo passo**: `@po *validate-story-draft 4.3` (re-validação obrigatória pela mudança HIGH). | River (@sm) |
